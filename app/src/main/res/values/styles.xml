<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/widget_default_green</item>
        <item name="colorPrimaryDark">@color/widget_default_green_darker</item>
        <item name="colorAccent">@color/widget_default_white</item>
    </style>

    <style name="Wifi_42" parent="AppTheme" />

    <style name="wifi42" parent="android:Theme.DeviceDefault.Light" />

</resources>
